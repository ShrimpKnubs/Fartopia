// File: EmergentKingdoms/src/Entities/Entity.cpp
#include "Entity.h"

namespace Entities {

// Currently no implementation needed in .cpp for the base Entity
// if all its methods are inline (like the constructor) or pure virtual.
// The virtual destructor provided in the header (defaulted) is sufficient.
// Add specific method implementations here if Entity gets more complex
// non-inline functions in the future.

} // namespace Entities