// File: EmergentKingdoms/src/World/Systems/Mountains/MountainColors.h
#pragma once
#include <SFML/Graphics.hpp>

namespace World {
namespace Systems {
namespace Mountains {
namespace Colors {

// ===== TASTEFUL ROCK & MOUNTAIN COLORS =====
const sf::Color ROCK_DARK_BASE = sf::Color(72, 68, 62);           // Deep weathered stone
const sf::Color ROCK_MID_SLOPE = sf::Color(95, 92, 85);           // Mid-tone stone
const sf::Color ROCK_LIGHT_EXPOSED = sf::Color(125, 122, 115);    // Exposed lighter rock
const sf::Color ROCK_PEAK = sf::Color(142, 138, 132);             // Peak stone color

// ===== REFINED SNOW COLORS =====
const sf::Color SNOW_SHADOW = sf::Color(188, 195, 205);           // Snow in shadow
const sf::Color SNOW_BRIGHT = sf::Color(218, 225, 235);           // Bright snow areas

} // namespace Colors
} // namespace Mountains
} // namespace Systems
} // namespace World